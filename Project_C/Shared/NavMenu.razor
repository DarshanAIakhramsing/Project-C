<div class="top-row pl-4 navbar navbar-dark">
    <a class="navbar-brand" href="index">CIMSOLUTIONS</a>
    <button class="navbar-toggler" @onclick="ToggleNavMenu">
        <span class="navbar-toggler-icon"></span>
    </button>
</div>

<div class="@NavMenuCssClass" id="nav" @onclick="ToggleNavMenu">
    <ul class="nav flex-column">
        <li id="Index" class="nav-item px-3">
            <NavLink class="nav-link" id="Index" href="index">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </li>
        <li class="nav-item px-3" id="SessieOverzicht">
            <NavLink class="nav-link" name="Sessie Overzicht" href="sessieoverzicht">
                <a class="oi oi-list-rich" aria-hidden="true"></a> Sessie overzicht
            </NavLink>
        </li>

        <AuthorizeView Roles="Organisator, Admin">
            <li class="nav-item px-3">
                <NavLink class="nav-link" href="sessies">
                    <span class="oi oi-list-rich" aria-hidden="true"></span> Sessie aanmaken
                </NavLink>
            </li>
        </AuthorizeView>
        <AuthorizeView Roles="Admin">
            <li class="nav-item px-3">
                <NavLink class="nav-link" href="medewerkers">
                    <span class="oi oi-list-rich" aria-hidden="true"></span> Medewerkers
                </NavLink>
            </li>
        </AuthorizeView>
    </ul>
</div>

@code {
    private bool collapseNavMenu = true;

    private string NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
